-- CREATE table EMP(empid INTEGER,
--             empname varchar(50),
--             empaddr TEXT);

-- insert into EMP(empid, empname, empaddr)
-- values(1, 'King', 'Baker street');

-- insert into EMP(empid, empname, empaddr)
-- values(2, 'Mark', 'chawkbazar');

select *
from emp;

